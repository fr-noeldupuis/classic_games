import 'package:flutter/material.dart';

class SudokuGame extends StatelessWidget {
  const SudokuGame({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Sudoku Game"),
      ),
    );
  }
}
